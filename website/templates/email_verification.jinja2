<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verification</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #e8f5e9, #c8e6c9);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .container {
            max-width: 500px;
            animation: fadeIn 0.5s ease-in-out;
        }
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 8px 30px rgba(0, 100, 0, 0.1);
            overflow: hidden;
        }
        .card-header {
            background: linear-gradient(135deg, #00c853, #64dd17);
            color: white;
            padding: 20px;
            text-align: center;
            font-size: 1.5rem;
            font-weight: bold;
            animation: pulse 2s infinite;
        }
        .card-body {
            padding: 30px;
        }
        .btn-primary {
            background: linear-gradient(135deg, #00c853, #64dd17);
            border: none;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0, 200, 83, 0.4);
        }
        .btn-light {
            border: 1px solid #ddd;
            transition: transform 0.2s ease;
        }
        .btn-light:hover {
            transform: translateY(-2px);
        }
        .message {
            font-size: 1.1rem;
            color: #555;
            margin-bottom: 20px;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }
    </style>
</head>
<body class="d-flex justify-content-center align-items-center vh-100">
    <div class="container">
        <div class="card">
            <div class="card-header">
                Email Verification
            </div>
            <div class="card-body text-center">
                <strong class="message">{{ message }}</strong>
                <br><br>
                
                {% if success %}
                    <a href="{{ url_for('user_auth.admin_login_render_template') }}" class="btn btn-primary btn-lg w-100 mt-3">
                        Proceed to Login
                    </a>
                {% else %}
                    
                    
                    <form action="{{ url_for('user_auth.resend_verification_email') }}" method="post" class="mt-3">
                        <div class="input-group mb-3">
                            <input type="email" name="emailT" value="{{ email }}" class="form-control form-control-lg" placeholder="Email" required>
                            <button type="submit" class="btn btn-primary btn-lg">
                                Resend Verification Email
                            </button>
                        </div>
                    </form>
                    <div class="d-grid gap-2 mb-3">
                        <a href="{{ url_for('user_auth.admin_login_render_template') }}" class="btn btn-light btn-lg">
                            Go to Login Page
                        </a>
                        <a href="{{ url_for('user_auth.user_signup') }}" class="btn btn-light btn-lg">
                            Go to Sign Up Page
                        </a>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>